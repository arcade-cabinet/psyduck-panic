# Auto-install peers to ensure peer dependencies are installed
auto-install-peers=true

# Prefer the newest compatible version when resolving peer dependencies
# This ensures react-reconciler@0.31.0 (React 19 compatible) is chosen over 0.29.2 (React 18)
resolution-mode=highest
